export type DateFormat = {
    year: "numeric";
    month: "short";
};

export type EducationInfo = {
    name: string;
    degree: string;
    startDate: number;
    endDate: number;
    description: string;
    picture: string;
};
